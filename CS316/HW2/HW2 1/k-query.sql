(SELECT thing2.name, min, bar FROM (SELECT name, MIN(price) FROM Beer LEFT OUTER JOIN Serves ON Beer.name = Serves.beer GROUP BY name) AS thing2 LEFT OUTER JOIN Serves ON min=price AND thing2.name = Serves.beer) EXCEPT (SELECT name, min, bar FROM (SELECT thing2.name, min, bar FROM (SELECT name, MIN(price) FROM Beer LEFT OUTER JOIN Serves ON Beer.name = Serves.beer GROUP BY name) AS thing2 LEFT OUTER JOIN Serves ON min=price AND thing2.name = Serves.beer) AS thing3 WHERE name IS NULL);